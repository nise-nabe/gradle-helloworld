{
  "private": true,
  "scripts": {
    "dev": "nuxi dev",
    "build": "nuxi build",
    "generate": "NODE_ENV=production nuxi generate",
    "generate_win": "set NODE_ENV=production && nuxi generate",
    "playwrightDeps": "playwright install-deps",
    "start": "nuxi preview",
    "prepare": "nuxi prepare",
    "test": "nuxi prepare && nuxi test"
  },
  "dependencies": {
    "nuxt": "^3.0.0-rc.6",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@nuxt/test-utils": "^3.0.0-rc.7",
    "playwright": "^1.23.4",
    "ts-node": "^10.9.1",
    "vitest": "^0.21.0"
  },
  "packageManager": "yarn@3.2.0"
}
